services:
  - type: web
    name: meus-pets
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT

  - type: postgres
    name: banco-pets
    plan: free
